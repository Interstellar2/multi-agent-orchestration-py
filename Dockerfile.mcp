FROM python:3.12-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY . .

# MCP Server 默认以 SSE 模式运行，端口 18080
EXPOSE 18080

CMD ["python", "-m", "mcp_bridge.server.demo_server", "--transport", "sse", "--host", "0.0.0.0", "--port", "18080"]
